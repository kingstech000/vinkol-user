/// Font families bundled with the app (declared under `flutter: fonts:` in pubspec.yaml).
///
/// Fonts ship in the binary; nothing is fetched at runtime.
abstract final class AppFonts {
  /// Montserrat, all nine weights plus italics, from `assets/fonts/Montserrat/static/`.
  static const String montserrat = 'Montserrat';
}
